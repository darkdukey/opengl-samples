cmake_minimum_required(VERSION 3.10)
project(grape)

# include conan scripts
include(${CMAKE_BINARY_DIR}/conanbuildinfo.cmake)
include(${CMAKE_BINARY_DIR}/conan_paths.cmake)
conan_basic_setup()

file(GLOB SRC_FILES "src/*.h" "src/*.cpp" )

add_executable(grape ${SRC_FILES})
target_link_libraries(grape ${CONAN_LIBS})

# # Src and Include folders
# file(GLOB_RECURSE SRC_FILES "src/*.cpp" "src/*.h")
# include_directories("include")

# # Build Library
# # add_executable(canvas ${SRC_FILES})
# add_library(grape ${SRC_FILES})
# target_compile_features(grape PRIVATE cxx_std_11)
# target_link_libraries(grape ${CONAN_LIBS})

# target_include_directories(grape
#     PUBLIC 
#         $<INSTALL_INTERFACE:include>   
#     PRIVATE
#         ${CMAKE_SOURCE_DIR}/src
# )

# # Build Samples
# option (BUILD_SAMPLES "Build the samples" ON)
# if (BUILD_SAMPLES)
#     add_subdirectory(samples)
# endif()
